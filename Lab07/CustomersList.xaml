<Window x:Class="Lab07.CustomersList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab07"
        mc:Ignorable="d"
        Title="CustomersList" Height="450" Width="800">
    <Grid Background="SpringGreen">
        <!-- Define las filas del Grid -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="dgCustomersList" IsReadOnly="True" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <Button Grid.Row="1" Width="100" Height="30" Margin="5" Background="Red" FontWeight="Bold" Foreground="White" Click="Button_Click" HorizontalAlignment="Center">EXIT</Button>
    </Grid>
</Window>